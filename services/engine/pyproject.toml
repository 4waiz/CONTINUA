[project]
name = "continua-engine"
version = "2.0.0"
description = "CONTINUA network simulator, controller and experiment runner"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["../../tests/engine"]
pythonpath = ["."]
addopts = "-q"
