{
  "name": "@continua/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared, framework-free type contracts between the CONTINUA scene runtime and the (Phase 2) network engine.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./engine": "./src/engine.ts",
    "./world.json": "./world.json"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
